# Production Configuration for AWS EC2
server.port=${PORT:8081}

# Database Configuration - MySQL (AWS RDS or local)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://pet-co.ct2o4qy2wabp.ap-south-1.rds.amazonaws.com:3306/pet_co?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:admin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Password123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration for MySQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Disable data.sql initialization for production
spring.sql.init.mode=never

# File upload configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.com.eduprajna=INFO
logging.level.pet-co=WARN
logging.level.org.springframework.web=INFO

# Health check endpoint
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

# Email Configuration (Production) - SendGrid
spring.mail.host=smtp.sendgrid.net
spring.mail.port=587
spring.mail.username=apikey
spring.mail.password=${SENDGRID_API_KEY}
sendgrid.api.key=${SENDGRID_API_KEY}

# Payment Configuration (Production) - Razorpay
razorpay.keyId=${RAZORPAY_KEY_ID}
razorpay.keySecret=${RAZORPAY_KEY_SECRET}

# Application Configuration (Production)
app.frontend.url=http://3.110.55.100
app.email.from=${EMAIL_FROM:PETCO <nishmitha928@gmail.com>}

aws.region=${AWS_REGION:ap-south-1}
aws.s3.bucket=${AWS_S3_BUCKET:database-images-pet-co}