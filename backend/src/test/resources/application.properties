# Use MySQL for tests - same as dev environment
spring.datasource.url=jdbc:mysql://pet-co.ct2o4qy2wabp.ap-south-1.rds.amazonaws.com:3306/pet_co?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=admin
spring.datasource.password=Password123

# Hibernate settings for tests
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=false

# Email Configuration for tests (SendGrid)
sendgrid.api.key=${SENDGRID_API_KEY:test-sendgrid-key}
app.email.from=PETCO <noreply@pet-co.com>
app.frontend.url=http://localhost:5173

# AWS S3 Configuration for tests (disabled by not setting aws.region)
# Uncomment the lines below if you want to test S3 functionality during tests
# aws.region=ap-south-1
# aws.s3.bucket=test-bucket

# Reduce logs during test runs
logging.level.org.springframework=ERROR
logging.level.org.hibernate=ERROR
